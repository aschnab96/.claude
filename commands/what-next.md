## Step 0 — Scan this conversation for loose ends
Before looking at git history or the backlog, check whether the current conversation contains any unresolved proposals: items flagged as "still outstanding," "worth a follow-up," "deferred," "open item," or "next step." If any exist:
1. List them clearly
2. For each, recommend: handle now / add to BACKLOG.md / discard
3. If adding to BACKLOG.md, propose the entry text and wait for approval before writing it

Only proceed to Step 1 once loose ends are resolved or explicitly deferred.

## Step 1 — Suggest the next task
Suggest one high-value next task based on recent git history and the current state of the codebase. Then advise on where to do it:

- **This chat:** Say whether to run `/compact` first to free up context, or proceed directly.
- **New chat:** Provide the exact opening prompt I should paste to start a new chat with the right context, and recommend which model tier (speed vs. reasoning) suits the task.

This is a routing decision, not a learning task — be concise.
